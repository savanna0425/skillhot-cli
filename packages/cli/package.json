{
  "name": "@skillhot/cli",
  "version": "0.1.0",
  "description": "Local, explainable discovery CLI for SkillHot agent skills.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/savanna0425/skillhot-cli.git",
    "directory": "packages/cli"
  },
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "skillhot": "./dist/main.js"
  },
  "files": [
    "dist",
    "data"
  ],
  "dependencies": {
    "@skillhot/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "pnpm run build && tsc --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --esModuleInterop --skipLibCheck --rootDir test --outDir .test-dist test/*.test.ts && node --test .test-dist/*.test.js",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  }
}
